package org.valkyrienskies.core.api.ships;

/**
 * Compile-only stub for the VS mixin callback descriptor.
 * Excluded from the produced jar; the real interface is supplied by Valkyrien Skies.
 */
public interface ClientShip {
}
